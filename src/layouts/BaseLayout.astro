---
import Foundation from "./Foundation.astro";

interface Props {
  title?: string;
  description?: string;
  variant?: 'default' | 'blog' | 'minimal';
}

const { title, description, variant = 'default' } = Astro.props;

const layoutClasses = {
  default: "min-h-screen bg-white",
  blog: "min-h-screen bg-white",
  minimal: "min-h-screen bg-slate-50"
};
---

<Foundation title={title} description={description} class={layoutClasses[variant]}>
  <slot name="header" slot="header" />
  <main class="flex-1">
    <slot />
  </main>
</Foundation>

<style>
  @reference "tailwindcss";
</style>